# 7/18/2026 TODOs

## 1.

Image background option
Either adding an image as a background for the final export, or as a trace image
Configurable opacity and other tunings


## 2.

UX for selecting characters (both fullscreen mode and non full screen)
Scrollable area, scrolling down you are able to scroll to each section of character types (all within one scroll area),
and still keeping buttons/items to switch between (now would just auto scroll to that section being in scroll view)

## 3. 

Color wheel (both fullscreen and non) color selection feature.
Expand on very basic color selection now to be a full feature color selection (RGBA, HEX, Color wheel selection)

## 4.

eye dropper icon is too similar to pencil icon. we can use a regular eyedropper icon instead of the designed one we added. 

## 5.

Shape section of subtool options fixes
Add detail header for this section "Shape"
"None" option currently acts as a square (size =2 -> 4 cells (2x2) shape). Should be a true count of cells for size for "None", i guess for now initially we just do a horizontal line of the size

## 6. 

